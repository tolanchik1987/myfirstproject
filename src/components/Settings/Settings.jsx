import React from "react";
import classes from "./Settings.module.css";

const Settings = (props) => {
   return (
       <div className={classes.conteiner}>Settings</div>
   )
}

export default Settings;
